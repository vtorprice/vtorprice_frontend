import { createCompanyActivitiesFormState } from '@box/features/settings/company_activity_types/lib';

const processorFormState = createCompanyActivitiesFormState(2, 'COMPANY_TYPE_PROCESSOR_FORM');

export {
  processorFormState,
};
